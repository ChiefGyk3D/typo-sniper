# Typo Sniper Requirements
# Python 3.10+
#
# SECURITY NOTE: All versions are pinned to protect against supply chain attacks
# (e.g. Shai-Hulud and similar compromised library incidents). These versions
# have been verified as secure and working correctly together.
#
# Feel free to experiment with newer versions in a test environment, but always
# review security advisories and changelogs before upgrading in production.

# Core dependencies
dnstwist==20250130
python-whois==0.9.6
PyYAML==6.0.3

# Required by dnstwist for DNS resolution. Without it dnstwist silently falls
# back to socket lookups: no MX or NS records are collected, mxcheck becomes a
# no-op, and more permutations come back as unresolvable error sentinels.
dnspython==2.8.0

# Excel support
openpyxl==3.1.5

# Rich console output
rich==15.0.0

# Async HTTP for threat intelligence lookups
aiohttp==3.14.3

# Local environment variables
python-dotenv==1.2.3

# Secrets management (optional)
# Uncomment the provider you want to use:

# Doppler secrets manager (https://doppler.com)
# doppler-sdk==1.3.0

# AWS Secrets Manager (https://aws.amazon.com/secrets-manager/)
# boto3==1.40.56
# botocore==1.40.56
